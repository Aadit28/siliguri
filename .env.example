# Copy to .env and fill in. Get values from Supabase dashboard -> Settings -> API.
EXPO_PUBLIC_SUPABASE_URL=https://YOUR_PROJECT.supabase.co
EXPO_PUBLIC_SUPABASE_ANON_KEY=your-anon-public-key
EXPO_PUBLIC_API_BASE_URL=http://localhost:8788

# Server-side only. Used by scripts/seed.mjs and Vercel API routes. Never expose in the app.
SUPABASE_SERVICE_ROLE_KEY=your-service-role-secret

# Optional server-side AI planner for /api/assistant/plan. Leave blank to use the local planner.
# Preferred: DeepSeek V4 Flash via OpenCode Zen/Go (OpenAI chat-completions compatible).
DEEPSEEK_API_KEY=your-opencode-zen-api-key
DEEPSEEK_BASE_URL=https://opencode.ai/zen/go/v1
DEEPSEEK_MODEL=deepseek-v4-flash

# Fallback planner + vision (photo attachments). Leave blank to skip.
OPENAI_API_KEY=your-openai-api-key
OPENAI_MODEL=gpt-4.1-mini

# Daily AI quota knobs (requests/24h). Defaults: 120 per user, 1200 global.
# AI_USER_DAILY_MAX=120
# AI_GLOBAL_DAILY_MAX=1200
